sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface
gradio>=6.9.0

# Document ingestion — pdfplumber for RMP PDFs in raw/
pdfplumber==0.11.4

# Hybrid retrieval — BM25 keyword search
rank-bm25==0.2.2
